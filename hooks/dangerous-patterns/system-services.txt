# system-services.txt - System service and power operations

# systemd service control
systemctl[[:space:]]+stop
systemctl[[:space:]]+disable
systemctl[[:space:]]+mask
systemctl[[:space:]]+kill
systemctl[[:space:]]+reset-failed

# systemd system operations
systemctl[[:space:]]+reboot
systemctl[[:space:]]+poweroff
systemctl[[:space:]]+halt
systemctl[[:space:]]+suspend
systemctl[[:space:]]+hibernate
systemctl[[:space:]]+hybrid-sleep
systemctl[[:space:]]+daemon-reexec

# Traditional service command
service[[:space:]]+.*[[:space:]]+stop
service[[:space:]]+.*[[:space:]]+kill

# macOS launchctl
launchctl[[:space:]]+unload
launchctl[[:space:]]+remove
launchctl[[:space:]]+disable
launchctl[[:space:]]+kill
launchctl[[:space:]]+bootout

# Power commands
shutdown[[:space:]]+
reboot([[:space:]]|$)
halt([[:space:]]|$)
poweroff([[:space:]]|$)
init[[:space:]]+[06]

# SysV init
update-rc.d[[:space:]]+.*[[:space:]]+remove
update-rc.d[[:space:]]+.*[[:space:]]+disable
chkconfig[[:space:]]+.*[[:space:]]+off
chkconfig[[:space:]]+--del

# Supervisor
supervisorctl[[:space:]]+stop
supervisorctl[[:space:]]+remove
supervisorctl[[:space:]]+shutdown

# PM2 (Node.js process manager)
pm2[[:space:]]+delete
pm2[[:space:]]+stop
pm2[[:space:]]+kill
pm2[[:space:]]+flush

# Upstart (legacy)
stop[[:space:]]+[a-z]
initctl[[:space:]]+stop

# BSD rc.d
rcctl[[:space:]]+disable
rcctl[[:space:]]+stop

# OpenRC
rc-update[[:space:]]+del
rc-service[[:space:]]+.*[[:space:]]+stop

# runit
sv[[:space:]]+down
sv[[:space:]]+force-stop

# s6
s6-svc[[:space:]]+-d
s6-svc[[:space:]]+-k

# macOS-specific
pmset[[:space:]]+.*sleep
osascript[[:space:]]+-e.*shutdown
osascript[[:space:]]+-e.*restart
